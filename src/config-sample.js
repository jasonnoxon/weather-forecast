// Rename this file to config.js
// and add the for the apixu.com api
// The DAYS setting is the number of days to fetch

const config = {
  KEY: '< YOUR API KEY>',
  DAYS: 7
};

export default config;
